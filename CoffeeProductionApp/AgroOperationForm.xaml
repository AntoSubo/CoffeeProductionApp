<Window x:Class="CoffeeProductionApp.AgroOperationForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Агротехническая операция" Height="480" Width="500" WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Плантация:" FontWeight="Bold" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboPlantation" Grid.Row="0" Grid.Column="1" Margin="5" Height="30" DisplayMemberPath="Value" SelectedValuePath="Key"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Тип операции:" FontWeight="Bold" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboOperationType" Grid.Row="1" Grid.Column="1" Margin="5" Height="30" DisplayMemberPath="Value" SelectedValuePath="Key"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата операции:" FontWeight="Bold" VerticalAlignment="Center"/>
        <DatePicker x:Name="dpOperationDate" Grid.Row="2" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Материалы:" FontWeight="Bold" VerticalAlignment="Top"/>
        <TextBox x:Name="txtMaterials" Grid.Row="3" Grid.Column="1" Margin="5" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Замечания:" FontWeight="Bold" VerticalAlignment="Top"/>
        <TextBox x:Name="txtRemarks" Grid.Row="4" Grid.Column="1" Margin="5" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Margin="10" Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Margin="10" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>