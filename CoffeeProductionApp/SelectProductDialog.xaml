<Window x:Class="CoffeeProductionApp.SelectProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор продукции" Height="250" Width="400" WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Продукт:" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboProduct" Grid.Row="0" Grid.Column="1" Margin="5" Height="30" DisplayMemberPath="Value" SelectedValuePath="Key"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Количество:" VerticalAlignment="Center"/>
        <TextBox x:Name="txtQuantity" Grid.Row="1" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Цена:" VerticalAlignment="Center"/>
        <TextBox x:Name="txtPrice" Grid.Row="2" Grid.Column="1" Margin="5" Height="30"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
            <Button x:Name="btnOK" Content="OK" Width="80" Margin="10" Click="btnOK_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="80" Margin="10" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>