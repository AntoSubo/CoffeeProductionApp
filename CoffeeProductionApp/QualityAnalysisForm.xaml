<Window x:Class="CoffeeProductionApp.QualityAnalysisForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Анализ качества" Height="500" Width="550" WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Дата анализа:" FontWeight="Bold" VerticalAlignment="Center"/>
        <DatePicker x:Name="dpAnalysisDate" Grid.Row="0" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Тип образца:" FontWeight="Bold" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboSampleType" Grid.Row="1" Grid.Column="1" Margin="5" Height="30">
            <ComboBoxItem Content="Зеленое зерно"/>
            <ComboBoxItem Content="Готовая продукция"/>
        </ComboBox>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Партия зеленого зерна:" FontWeight="Bold" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboGreenBatch" Grid.Row="2" Grid.Column="1" Margin="5" Height="30" DisplayMemberPath="Value" SelectedValuePath="Key" IsEnabled="False"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Партия готовой продукции:" FontWeight="Bold" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboFinishedBatch" Grid.Row="3" Grid.Column="1" Margin="5" Height="30" DisplayMemberPath="Value" SelectedValuePath="Key" IsEnabled="False"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Параметры анализа:" FontWeight="Bold" VerticalAlignment="Top"/>
        <TextBox x:Name="txtParameters" Grid.Row="4" Grid.Column="1" Margin="5" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Оценка каппинга:" FontWeight="Bold" VerticalAlignment="Center"/>
        <TextBox x:Name="txtCuppingScore" Grid.Row="5" Grid.Column="1" Margin="5" Height="30"/>

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Заключение:" FontWeight="Bold" VerticalAlignment="Center"/>
        <ComboBox x:Name="cboConclusion" Grid.Row="6" Grid.Column="1" Margin="5" Height="30">
            <ComboBoxItem Content="Годен"/>
            <ComboBoxItem Content="Брак"/>
            <ComboBoxItem Content="Требуется повторный анализ"/>
        </ComboBox>

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Margin="10" Click="btnSave_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Margin="10" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>